This is the server part of the alarm clock project.

This is built for an ESP8266, which is creates its own wifi and listens to a certain port waiting for a TCP packet from the client, then turns on a GPIO pin.

This uses Espressif's SDK for ESP8266.
